import 'package:flutter/material.dart';

class SuppliedContainer extends StatelessWidget {
  const SuppliedContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
