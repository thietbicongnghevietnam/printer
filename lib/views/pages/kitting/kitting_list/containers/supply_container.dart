import 'package:flutter/material.dart';

class SupplyContainer extends StatelessWidget {
  const SupplyContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
